logging:
  level:
    org.springframework: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  security:
    user:
      name: username
      password: passw0rd
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true
